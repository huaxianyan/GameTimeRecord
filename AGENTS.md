# AGENTS.md

## 语言

始终使用简体中文回答，代码、命令和专有名词除外。

## 项目定位

GameTimeRecord 是仅支持 Windows 10 及更高版本的本地游戏时间记录工具。使用各平台原生方案是长期方向，本项目不为假设中的跨平台版本引入跨平台 UI 或共享平台抽象。

## 技术方向

- 使用 C#、.NET 10、WPF、CommunityToolkit.Mvvm、SQLite 和 xUnit
- 使用简单的模块化单体，不照搬为跨平台准备的多层项目结构
- 时间戳以 UTC Unix 秒保存，按 Windows 当前时区显示为 `yyyy-MM-dd HH:mm:ss`
- 应用数据保存在 `%LOCALAPPDATA%\GameTimeRecord`

## 业务规则

- 点击「开始游戏」增加一次游玩次数，「恢复」不增加
- 允许多个游戏同时计时，每个游戏最多有一个未结束会话
- 有效时长只计算「开始或恢复」到「暂停或结束」的区间
- 暂停状态可以直接结束，暂停期间不计入时长
- 首次游玩时间取最早的开始时间，最后游玩时间取最晚的结束时间
- 累计时长和游玩次数复制纯数字，时间复制本地显示值
- 历史记录允许修改，但事件类型和时间必须保持有效顺序
- 正常退出前，所有正在计时的游戏必须先暂停或结束；暂停会话可以保留到下次启动

## 验证

- 核心时间规则使用固定的字面量时间戳测试，不用生产计算逻辑生成期望值
- SQLite 数据流程至少保留一个通过真实数据库入口执行的测试
- 发布标签使用 `v<major>.<minor>.<patch>`，发布说明位于 `docs/release-notes/<标签>.md`
