<Window x:Class="GameTimeRecord.App.Views.GameEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="游戏信息"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,7,12,12" Text="游戏名称" />
        <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,7,12,12" Text="别名" />
        <TextBox x:Name="AliasBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,7,12,12" Text="游戏平台" />
        <TextBox x:Name="PlatformBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" />

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,7,12,12" Text="备注" />
        <TextBox x:Name="NotesBox"
                 Grid.Row="3"
                 Grid.Column="1"
                 Height="90"
                 Margin="0,0,0,20"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="88" Margin="0,0,8,0" IsCancel="True" Content="取消" />
            <Button Width="88" IsDefault="True" Click="Save_Click" Content="保存" />
        </StackPanel>
    </Grid>
</Window>
