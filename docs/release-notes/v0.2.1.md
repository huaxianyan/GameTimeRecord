# GameTimeRecord v0.2.1

## 修复

- 修复点击复制统计数据时应用可能直接退出的问题。剪贴板被其他程序占用导致写入失败时，不再中断进程
- 剪贴板被占用时自动等待并重试，通常不需要手动再点一次
- 确实无法写入时给出提示，并附上剪贴板的占用情况，便于定位是哪个程序在占用

## 界面改进

- 复制按钮点击后直接在按钮上显示「已复制」，若干秒后自动恢复，不再占用底部的提示文字
- 复制结果不再区分具体的写入方式，只看成功与否

## 数据兼容

本版本沿用原有 SQLite 数据结构和数据目录，升级后可以直接读取已有游戏与游玩记录。
